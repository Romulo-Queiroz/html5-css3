'use strict';

const pesquisarchar= () =>{
    const nome = document.getElementById('nome').value;
    const url= `https://api.tibiadata.com`;
    d
    fetch(url).then(console.log);
    
    console.log(nome)
}

document.getElementById('nome')
.addEventListener('focusout', pesquisarchar);